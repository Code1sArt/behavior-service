# Safe first-production-rollout gate.
# This file contains no secrets and is read by the SSH deployment script.
DEPLOY_DATABASE_MODE="${DEPLOY_DATABASE_MODE:-migrate}"
DEPLOY_APP_MODE="${DEPLOY_APP_MODE:-release}"
